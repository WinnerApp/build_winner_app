name: build_winner_app
description: 自动化打包稳健医疗App工具.
version: 1.0.8
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.0

# Add regular dependencies here.
dependencies:
  path: ^1.8.0
  args: ^2.4.2
  process_run: ^0.13.3
  color_logger: ^1.0.2
  dio: ^5.4.0
  yaml: ^3.1.2
  darty_json_safe: ^1.0.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
executables:
  build_winner_app:
